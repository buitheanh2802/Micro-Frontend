import React from 'react';

type Props = {};

const App: React.FC<Props> = (props): React.ReactNode => {
    return (
        <React.Fragment>
            Hello React App
        </React.Fragment>
    )
};

export default App;